ARCHITECTURE SUMMARY

Driver        {{driver}}
Hosting       {{hosting}}
File tree     {{fileTreeNote}}

COST PREVIEW (estimated, per 100 questions)

  ~40  cache hits           no model
  ~15  route + refuse        no model
  ~40  retrieve + cite       small model
  ~5   synthesis + review    large model

  Gate: local stdio server  when a 2nd coding client appears
  Gate: remote server       after 2 asks from non-repo users

  Portability: nothing below the tool boundary is vendor-specific.

The mix is estimated — replaced by telemetry at phase 4.
